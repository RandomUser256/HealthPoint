//
//  FoundationModelTest.swift
//  HealthPoint
//
//  Created by Máximo on 4/11/26.
//
/*
import SwiftUI
import FoundationModels

struct GenerativeView: View {
    // Create a reference to the system language model.
    private var model = SystemLanguageModel.default


    var body: some View {
        switch model.availability {
        case .available:
            Text("Model available")
        case .unavailable(.deviceNotEligible):
            // Show an alternative UI.
            Text("Model unavailable on this device")
        case .unavailable(.appleIntelligenceNotEnabled):
            // Ask the person to turn on Apple Intelligence.
            Text("Please turn on Apple intelligence")
        case .unavailable(.modelNotReady):
            // The model isn't ready because it's downloading or because of other system reasons.
            Text("Model unavailable, waiting for download")
        case .unavailable(let _):
            // The model is unavailable for an unknown reason.
            Text("Model available for unknown reason")
        }
    }
}

*/
